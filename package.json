{
  "name": "iris-walmart-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "cd frontend && npm run build && cd ../functions && dotenv -e ../.env -- twilio serverless:deploy --override-existing-project"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
